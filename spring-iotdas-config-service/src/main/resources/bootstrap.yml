spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/compartilhado
  profiles:
     active: native

server:
  port: 8888

security:
    user:
      password: UmSbh9XLkkuqtUf2kyQC4bS
